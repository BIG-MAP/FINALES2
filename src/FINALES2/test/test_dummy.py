def test_dummy():
    """This method only exists for the GitHub action to work as this fails, if
    no tests are found. It must be removed once the actual tests are re-activated.
    """
    assert 1 == 1
